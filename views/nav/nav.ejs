<div>
  <nav class="navbar navbar-expand-lg navbar-dark bg-primary fixed-top">
    <div class="container-fluid">
      <a class="navbar-brand" href="/dashboard"><span class="h4 m-0 p-0">AppsFly</span></a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls=" navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
            <li class="nav-item">
                <a class="nav-link <% if (view === 'dashboard') { %> active <% } %> " aria-current="page" href="/dashboard">Dashboard</a>
              </li>
          <li class="nav-item">
            <a class="nav-link <% if (view === 'cotizador') { %>active<%} %>" aria-current="page" href="/cotizador">Cotizador</a>
          </li>
          <li class="nav-item">
            <a class="nav-link <% if (view === 'services') { %>active<%} %>" href="/servicios">Servicios</a>
          </li>
          <li class="nav-item">
            <a class="nav-link <% if (view === 'price-list') { %>active<%} %>" href="/lista-de-precios">Lista Precios</a>
          </li>
          <li class="nav-item" <% if (dataUser.userRol === 'r0' || dataUser.userRol === 'r3' ||  dataUser.userRol == undefined) { %> hidden <%} %> >
            <a class="nav-link <% if (view === 'users') { %>active<%} %>" href="/usuarios">Usuarios</a>
          </li>     
          <li class="nav-item">
            <a class="nav-link" href="/logout">Salir</a>
          </li>
        </ul>
      </div>      
    </div>
  </nav>
</div>

  <% if (typeof alertMessage !== 'undefined'){%>
      <div class="m-2 alert alert-<%= alertType %> alert-dismissible fade show" role="alert">
          <%= alertMessage %>
          <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
      </div>
  <% } %>        
